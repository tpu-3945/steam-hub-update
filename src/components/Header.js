import React from 'react';
import '../assets/Header.css';

const Header = () => (
  <header className="header">
    <h1 className="orbitron neon-title">STEAM HUB</h1>
    <p className="header-subtitle">
      Générateur de manifests et exploration d'AppID Steam, relax et immersif.
    </p>
  </header>
);

export default Header;
